services:
  qa-auditor:
    build:
      context: .
    ports:
      - "${HOST_PORT:-3000}:3000"
    env_file:
      - .env
    volumes:
      - ./templates:/app/templates:ro
    # Makes a host-installed MongoDB reachable from Linux containers using
    # the same host.docker.internal name used by Docker Desktop.
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      NODE_ENV: production
      PORT: 3000
      DEPLOYMENT_NAMESPACE: ${DEPLOYMENT_NAMESPACE:-development}
      AI_CACHE_TTL_MS: 21600000
      AI_CACHE_MAX_ENTRIES: 100
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:3000/healthz').then(r => process.exit(r.ok ? 0 : 1)).catch(() => process.exit(1))"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
